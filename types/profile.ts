// Re-export from the single source of truth to avoid drifting duplicates.
export type {
  ProfileBasics,
  FeaturedItem,
  ExperienceRole,
  ExperienceItem,
  EducationItem,
  ProfileProject,
  Certification,
  Profile,
} from "./index";
